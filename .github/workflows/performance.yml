# SPDX-FileCopyrightText: Glaucus contributors
#
# SPDX-License-Identifier: MIT OR Apache-2.0

name: Performance

# Multi-architecture benchmark run. `perf-matrix` expands one platform table
# into a job per NATIVE host -- Linux x86_64/aarch64 and macOS x86_64/aarch64 --
# each measuring the Criterion suite where it actually runs, and `perf-report`
# pivots the four result sets into one table.
#
# It MEASURES; it does not gate. The per-change regression gate against the
# `gh-pages` baseline is described at the bottom of this file and is not wired
# up: benchmark numbers from shared CI runners move by tens of percent between
# runs, which fails pull requests on phantom regressions. Every figure this
# workflow publishes from a GitHub-hosted runner is marked `degraded` for the
# same reason -- read ratios within a column, never absolute times across them.
#
# Security note: this workflow does not interpolate any user-controlled
# content (issue/PR titles, commit messages, head_ref, etc.) into `run:`
# steps. All `${{ }}` usages are either repo metadata in action inputs
# or conditional gates; dispatch inputs reach a shell only through `env:`.
#
# gh-pages setup, needed ONLY if the baseline gate at the bottom is restored:
#   1. Create an empty `gh-pages` branch:
#        git checkout --orphan gh-pages && git rm -rf . && \
#        git commit --allow-empty -m "init gh-pages" && \
#        git push origin gh-pages
#   2. In repo Settings -> Pages, point Pages at the `gh-pages` branch
#      (optional -- only needed to view the historical dashboard).
#   3. In repo Settings -> Actions -> General -> Workflow permissions,
#      ensure "Read and write permissions" is enabled (required for the
#      action to push the updated baseline).

concurrency:
  group: perf-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  # Auto-triggers disabled: the benchmark gate is flaky on shared CI runners
  # (noisy neighbours skew timings and fail PRs on phantom regressions). Run it
  # manually via the Actions tab / `gh workflow run performance.yml` when needed.
  # push:
  #   branches: [main]
  #   paths:
  #     - "crates/glaucus-core/**"
  #     - "crates/glaucus-serde/**"
  #     - "crates/glaucus/**"
  #     - "crates/glaucus-bench/**"
  #     - "Cargo.toml"
  #     - "Cargo.lock"
  #     - ".github/workflows/performance.yml"
  # pull_request:
  #   branches: [main]
  #   paths:
  #     - "crates/glaucus-core/**"
  #     - "crates/glaucus-serde/**"
  #     - "crates/glaucus/**"
  #     - "crates/glaucus-bench/**"
  #     - "Cargo.toml"
  #     - "Cargo.lock"
  #     - ".github/workflows/performance.yml"
  # The matrix is chosen HERE, at dispatch time, not hardcoded in the job. A
  # benchmark run is expensive -- the full suite is ~85 minutes per host, and
  # macOS minutes bill at 10x Linux -- so "measure everything" has to be a
  # deliberate choice rather than the only option. Every input has a default
  # that makes a bare `gh workflow run performance.yml` do the full sweep.
  workflow_dispatch:
    inputs:
      platforms:
        description: "Hosts to measure on"
        type: choice
        default: all
        options:
          - all
          - linux
          - macos
          - linux-x86_64
          - linux-aarch64
          - macos-x86_64
          - macos-aarch64
      toolchain:
        description: "Toolchain: `default` reads rust-toolchain.toml"
        type: choice
        default: default
        options:
          - default
          - stable
          - nightly
      mode:
        description: "`quick` trades Criterion's precision for wall-clock"
        type: choice
        default: full
        options:
          - full
          - quick
      bench:
        description: "One bench target, e.g. `scanner`; empty runs all seven"
        type: string
        default: ""

# Read-only at workflow scope. Every write this workflow needs is declared on
# the single job that performs it, below.
#
# The distinction is not cosmetic even with one job: a workflow-level grant is
# inherited by every job added later, so the second job to appear here would
# silently receive push access to gh-pages and comment access to pull requests
# without anyone writing that down. Declaring it per job makes the next
# contributor state the need explicitly, which is the point of least privilege.
permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  # `mise run` installs [tools] before every task, which overrides mise-action's
  # `install: false`: the Supply Chain job spent 8m57s having `mise run comply`
  # build all 26 pinned tools from source, because mise looks for cargo-binstall
  # at ~/.cargo/bin and CI has none, so its cargo backend compiles instead. The
  # gates below use mise for its config flags, not for provisioning --
  # setup-rust-env has already put every tool they need on PATH as a prebuilt
  # binary.
  MISE_TASK_RUN_AUTO_INSTALL: false
  # mise.toml's [env] does `GITHUB_TOKEN = {{env.GITHUB_TOKEN | default(value='')}}`,
  # so with nothing set it exports an EMPTY token -- worse than unset, since any
  # tool that reads it then makes unauthenticated, rate-limited API calls.
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN || github.token }}
  GITVERSION_SPEC: ${{ vars.GITVERSION_SPEC || '6.x' }}
  CRITERION_HOME: ${{ github.workspace }}/target/criterion
  REF: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.ref }}

jobs:
  # ──────────────────────────────────────────────
  #  1.  Setup — shared bootstrap for all jobs
  # ──────────────────────────────────────────────
  setup:
    name: Setup
    runs-on: ${{ vars.RUNS_ON_ARM64 || vars.RUNS_ON || 'ubuntu-latest' }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: "recursive"
          fetch-depth: 0
          fetch-tags: "true"
          ref: ${{ env.REF }}

      - name: Setup Environment
        uses: ./.github/actions/setup-rust-env

  # ──────────────────────────────────────────────
  #  2.  Version - shared GitVersion SemVer
  # ──────────────────────────────────────────────
  # GitVersion runs HERE and nowhere else in this workflow. It used to be three
  # steps inside `release` -- cache, setup, execute -- which meant the tool was
  # provisioned and the version recomputed inside a job that also tags, pushes
  # and publishes. version.yml owns that now, so there is one definition of what
  # the version is and one place that derives `majorMinor`, which the action
  # itself does not publish.
  #
  # A ROOT job -- no `needs`, no `if:`. It derives the version from tags and
  # commit distance, so it wants the checkout and nothing else; waiting on `build`
  # would serialise it behind the whole test-and-build chain for no reason.
  #
  # The absence of an `if:` is load-bearing, not an oversight. `build` depends on
  # this job and `build` runs on pull requests, and GitHub skips the dependents of
  # a skipped job -- so gating this on `push` to the default branch would skip
  # `build` on every pull request. A job that only `release` consumed could be
  # conditional; one that `build` consumes cannot.
  version:
    name: Version
    permissions:
      contents: read
    uses: ./.github/workflows/version.yml

  # ──────────────────────────────────────────────
  #  3.  Perf matrix — ONE platform table, expanded at dispatch time
  # ──────────────────────────────────────────────
  # The generator. It exists because `runs-on` is the ONLY thing that decides
  # which architecture a job measures: a matrix that crosses `os: [ubuntu-latest,
  # macos-15]` with `arch: [x86_64, aarch64]` produces four jobs on TWO distinct
  # machines -- the `aarch64` half lands on the same x86_64 runner as its sibling
  # and reports x86_64 timings under an aarch64 label. Cross-compiling does not
  # rescue it either: a benchmark has to RUN, and an aarch64 binary does not run
  # on an x86_64 host. So arch is not a dimension here; it is a property of the
  # runner label, and this table is the mapping.
  #
  # Native runner labels (verified 2026-08-26):
  #   ubuntu-24.04       Linux x86_64
  #   ubuntu-24.04-arm   Linux aarch64  (GA; free for public repos)
  #   macos-15-intel     macOS x86_64   (the LAST Intel image -- available until
  #                                      August 2027, after which GitHub hosts no
  #                                      x86_64 macOS at all)
  #   macos-15           macOS aarch64  (Apple silicon)
  #
  # Every label is overridable through a repo variable, which is the point of
  # keeping them in data rather than in `runs-on`. Benchmarks on shared runners
  # are noisy enough that the numbers carry a `degraded` marker (see the
  # preflight step below); pointing PERF_RUNNER_* at self-hosted metal is how a
  # run becomes decision-grade without editing this file.
  perf-matrix:
    name: Perf Matrix
    # A ROOT job. It expands a table with jq -- no checkout, no toolchain, no
    # dependency on `setup`, whose cache is keyed to ONE host and therefore has
    # nothing to offer four different ones.
    runs-on: ${{ vars.RUNS_ON_ARM64 || vars.RUNS_ON || 'ubuntu-latest' }}
    timeout-minutes: 5
    outputs:
      include: ${{ steps.expand.outputs.include }}
      count: ${{ steps.expand.outputs.count }}
    steps:
      - name: Expand the platform table
        id: expand
        # Through the environment rather than `${{ }}` inside the script body:
        # `platforms` is a dispatch input, and interpolating one straight into a
        # shell is the injection shape the header note above forbids. `choice`
        # inputs constrain the value, but the rule does not bend for the inputs
        # that happen to be safe today.
        env:
          SELECT: ${{ inputs.platforms || 'all' }}
          CHANNEL: ${{ inputs.toolchain || 'default' }}
          RUNNER_LINUX_X86_64: ${{ vars.PERF_RUNNER_LINUX_X86_64 || 'ubuntu-24.04' }}
          RUNNER_LINUX_AARCH64: ${{ vars.PERF_RUNNER_LINUX_AARCH64 || 'ubuntu-24.04-arm' }}
          RUNNER_MACOS_X86_64: ${{ vars.PERF_RUNNER_MACOS_X86_64 || 'macos-15-intel' }}
          RUNNER_MACOS_AARCH64: ${{ vars.PERF_RUNNER_MACOS_AARCH64 || 'macos-15' }}
        run: |
          set -Eeuo pipefail

          include="$(
            jq -nc \
              --arg select "${SELECT}" \
              --arg channel "${CHANNEL}" \
              --arg linux_x86_64 "${RUNNER_LINUX_X86_64}" \
              --arg linux_aarch64 "${RUNNER_LINUX_AARCH64}" \
              --arg macos_x86_64 "${RUNNER_MACOS_X86_64}" \
              --arg macos_aarch64 "${RUNNER_MACOS_AARCH64}" '
                [
                  {key: "linux-x86_64",  label: "Linux x86_64",  os: "linux", arch: "x86_64",  runner: $linux_x86_64,  target: "x86_64-unknown-linux-gnu"},
                  {key: "linux-aarch64", label: "Linux aarch64", os: "linux", arch: "aarch64", runner: $linux_aarch64, target: "aarch64-unknown-linux-gnu"},
                  {key: "macos-x86_64",  label: "macOS x86_64",  os: "macos", arch: "x86_64",  runner: $macos_x86_64,  target: "x86_64-apple-darwin"},
                  {key: "macos-aarch64", label: "macOS aarch64", os: "macos", arch: "aarch64", runner: $macos_aarch64, target: "aarch64-apple-darwin"}
                ]
                | map(select($select == "all" or .os == $select or .key == $select))
                | map(. + {
                    channel: $channel,
                    # setup-rust-env reads rust-toolchain.toml when this is
                    # empty, which is what keeps that file the single source of
                    # truth for the default run.
                    toolchain: (if $channel == "default" then "" else $channel end)
                  })
              '
          )"

          count="$(jq 'length' <<<"${include}")"
          if [ "${count}" -eq 0 ]; then
            echo "::error::no platform matches '${SELECT}'; nothing to measure"
            exit 1
          fi

          echo "include=${include}" >> "$GITHUB_OUTPUT"
          echo "count=${count}" >> "$GITHUB_OUTPUT"

          {
            echo "### Performance matrix"
            echo
            echo "| Platform | Runner | Target | Toolchain |"
            echo "|:--|:--|:--|:--|"
            jq -r '.[] | "| \(.label) | `\(.runner)` | `\(.target)` | \(.channel) |"' <<<"${include}"
          } >> "$GITHUB_STEP_SUMMARY"

  # ──────────────────────────────────────────────
  #  4.  Lint — HK project lints
  # ──────────────────────────────────────────────
  lint:
    name: Lint
    # `setup` and `version` are the two roots and run in parallel; everything
    # downstream gates on both. Naming `version` in this first tier is what makes
    # a version failure stop the run here rather than surfacing 20 minutes later
    # in `build` -- and `tests` inherits the dependency transitively, so it does
    # not need repeating there.
    needs: [setup, version]
    runs-on: ${{ vars.RUNS_ON_ARM64 || vars.RUNS_ON || 'ubuntu-latest' }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: "recursive"
          fetch-depth: 0
          fetch-tags: "true"
          ref: ${{ env.REF }}

      - name: Setup Environment
        uses: ./.github/actions/setup-rust-env

      - name: Run HK Lints
        run: hk check --all

  # ──────────────────────────────────────────────
  #  5.  MSRV — compile at the DECLARED minimum
  # ──────────────────────────────────────────────
  # The job that keeps `rust-version` honest. Without it an MSRV is a claim, not
  # a constraint: every other job here builds with rust-toolchain.toml's pin, so
  # the declared floor can only ever drift UPWARD, and always silently -- the
  # pinned toolchain never disagrees with it. That is exactly how the workspace
  # came to advertise 1.97 while the code still compiled on 1.88, making every
  # routine toolchain bump an invisible breaking change for consumers (issue #3).
  msrv:
    name: MSRV
    # First tier, alongside `lint`/`sast`, rather than downstream of them: this
    # is one `cargo check`, and a broken floor should surface in three minutes
    # instead of behind the full test-and-build chain.
    needs: [setup, version]
    runs-on: ${{ vars.RUNS_ON_ARM64 || vars.RUNS_ON || 'ubuntu-latest' }}
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: "recursive"
          fetch-depth: 0
          fetch-tags: "true"
          ref: ${{ env.REF }}

      # READ the floor; never hardcode it. A literal version here would be a
      # second source of truth free to disagree with Cargo.toml -- the same class
      # of bug this job exists to catch, just relocated into the workflow.
      - name: Read declared MSRV from Cargo.toml
        id: msrv
        run: |
          set -euo pipefail
          version="$(sed -n 's/^rust-version[[:space:]]*=[[:space:]]*"\([^"]*\)".*/\1/p' Cargo.toml | head -n1)"
          if [ -z "$version" ]; then
            echo "::error file=Cargo.toml::no rust-version found in [workspace.package]"
            exit 1
          fi

          # Two files declare the floor to two different tools, and they have to
          # agree. Cargo.toml's `rust-version` is the promise to consumers and
          # what this job compiles against; .clippy.toml's `msrv` is what makes
          # clippy REJECT an API newer than the floor even though the pinned
          # toolchain offers it -- a check that catches at lint time what this
          # job only catches once the code is written.
          #
          # Nothing else compares them. Left unchecked, .clippy.toml silently
          # keeps linting against a floor the project abandoned, which is the
          # exact defect this job exists to prevent, one file over. A missing
          # `msrv` is failed too, not tolerated: dropping the key would quietly
          # switch clippy back to assuming the pinned toolchain.
          clippy_msrv="$(sed -n 's/^msrv[[:space:]]*=[[:space:]]*"\([^"]*\)".*/\1/p' .clippy.toml | head -n1)"
          if [ -z "$clippy_msrv" ]; then
            echo "::error file=.clippy.toml::no msrv key; it must declare msrv = \"${version}\" to match Cargo.toml"
            exit 1
          fi
          if [ "$clippy_msrv" != "$version" ]; then
            echo "::error file=.clippy.toml::msrv \"${clippy_msrv}\" disagrees with Cargo.toml rust-version \"${version}\""
            exit 1
          fi

          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "Declared MSRV: ${version} (Cargo.toml and .clippy.toml agree)"

      # Deliberately NOT ./.github/actions/setup-rust-env, which every other job
      # here uses. That action provisions the DEVELOPMENT environment -- mold,
      # clippy/rustfmt/llvm-tools, cargo-binstall, every mise `cargo:` tool,
      # comply and ignorefile -- and it builds that tooling with whatever
      # toolchain is active. Pointing it at the MSRV therefore tried to compile
      # the tooling with 1.88, and it died on the first tool that had moved on:
      #
      #   error: cannot install package `ignorefile-cli 0.1.0`, it requires
      #   rustc 1.95 or newer, while the currently active rustc version is 1.88.0
      #
      # None of that tooling is part of the promise made to consumers, and a gate
      # that fails because a lint helper wants a newer compiler reports nothing
      # about the MSRV. So this job installs rustc and cargo at the declared
      # floor and nothing else. rustup ships preinstalled on GitHub runners.
      #
      # Dropping the composite drops mold with it, which is why the step below
      # must name RUSTFLAGS. See there.
      #
      # No target cache either: the gate's worth is its reliability, not its
      # speed, it runs in the first tier in parallel, and `tests`/`build` set the
      # critical path regardless. Add one only if this ever becomes the slow job.
      - name: Install the declared MSRV toolchain
        env:
          MSRV: ${{ steps.msrv.outputs.version }}
        run: |
          set -euo pipefail
          rustup toolchain install "$MSRV" --profile minimal --no-self-update
          rustup run "$MSRV" rustc --version

      - name: Compile every publishable crate at the MSRV
        env:
          # Selects the toolchain installed above for every cargo/rustc call in
          # this step; it outranks rust-toolchain.toml. This is the override that
          # makes the job mean anything.
          RUSTUP_TOOLCHAIN: ${{ steps.msrv.outputs.version }}
          # REQUIRED, not tidiness. .cargo/config.toml's
          # `[target.x86_64-unknown-linux-gnu]` sets
          # `["-D", "warnings", "-C", "link-arg=-fuse-ld=mold"]`, and mold is
          # installed by setup-rust-env -- which this job deliberately does not
          # run. A RUSTFLAGS env var REPLACES that list rather than merging into
          # it, so naming the flag here keeps the warning policy byte-for-byte
          # while dropping a linker that is not on this runner.
          #
          # `cargo check` does NOT get a pass on that: it still builds and RUNS
          # build scripts, and still compiles proc-macro crates, both of which
          # link. This graph has ~30 of them (serde_derive, proc-macro2, quote,
          # wasm-bindgen-macro, ...). Delete this line and the job fails at the
          # first one, for a reason that has nothing to do with the MSRV.
          #
          # No `--target` is passed, so RUSTFLAGS reaches those host artifacts
          # too, which is exactly what makes the override work.
          #
          # Warnings stay denied AT THE FLOOR because `lint` cannot cover this:
          # it runs on the pinned toolchain, so a lint that fires on 1.88 but was
          # renamed or removed by the pin would go unseen. Verified clean -- the
          # check below passes with zero warnings on 1.88.
          #
          # The trade-off to watch as the floor and stable drift apart: an
          # `#[allow]` naming a lint 1.88 has never heard of raises
          # `unknown_lints`, which would fail a gate that blocks `release` for a
          # reason that says nothing about MSRV. If that day comes, set this to
          # "" rather than weakening the compile check -- `lint` still owns the
          # warning gate on the pinned toolchain.
          RUSTFLAGS: "-D warnings"
        run: |
          set -euo pipefail

          mise run test:all

          # Derive the crate list instead of maintaining `--exclude` flags:
          # cargo reports `publish` as [] for `publish = false` and null when
          # publishing is allowed, so this selects exactly what a consumer can
          # install from crates.io -- and a crate added tomorrow is covered the
          # day it lands rather than escaping the gate until someone notices.
          #
          # bench/fuzz/yaml-test-suite are excluded by that rule, which is the
          # point: dev-only crates drag in criterion, tarpaulin and a nightly
          # requirement, and their floors are not part of the promise made to
          # consumers. For the same reason this is `check`, not `test` -- running
          # the suite would let a dev-dependency dictate the published MSRV.
          mapfile -t names < <(
            cargo metadata --format-version 1 --no-deps \
              | jq -r '.packages[] | select(.publish != []) | .name' | sort
          )
          if [ "${#names[@]}" -eq 0 ]; then
            echo "::error::no publishable crates resolved; refusing to pass vacuously"
            exit 1
          fi
          packages=()
          for name in "${names[@]}"; do packages+=(--package "${name}"); done
          echo "MSRV $(rustc --version)"
          printf 'Checking %s\n' "${names[@]}"
          # `--all-features` because the floor has to hold for every feature a
          # consumer can switch on; a default-only check would let an optional
          # dependency raise the real MSRV without ever failing here.
          cargo check --locked --all-features "${packages[@]}"

  # ──────────────────────────────────────────────
  #  6.  Benchmark — one native run per platform
  # ──────────────────────────────────────────────
  benchmark:
    name: Bench (${{ matrix.label }})
    needs: [setup, version, perf-matrix]
    runs-on: ${{ matrix.runner }}
    # The full suite is ~85 minutes on a healthy host and the slowest platform
    # sets this, not the fastest.
    timeout-minutes: 150
    strategy:
      # A dead runner label or an OOM on one platform must not cancel the three
      # that are measuring fine -- partial results are still results, and
      # `perf-report` renders whatever arrives.
      fail-fast: false
      matrix:
        # No base keys: `include` alone expands to exactly the entries the
        # generator emitted, one job each.
        include: ${{ fromJSON(needs.perf-matrix.outputs.include) }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # No submodules and no history. The only submodule is the yaml-test-
          # suite conformance corpus, which nothing under crates/glaucus-bench
          # reads -- every fixture there is generated in code -- and cloning it
          # on four runners buys nothing. bench-to-json.py's `git rev-parse`
          # calls are satisfied by a depth-1 checkout.
          fetch-depth: 1
          ref: ${{ env.REF }}

      - name: Setup Environment
        uses: ./.github/actions/setup-rust-env
        with:
          toolchain: ${{ matrix.toolchain }}
          # Namespaced per platform AND per channel: these caches key on the
          # dependency set and the toolchain, and four hosts sharing one
          # namespace would otherwise trade artifacts they cannot use.
          cache-key-suffix: perf-${{ matrix.key }}-${{ matrix.channel }}

      # The guard that makes the matrix mean what it says. If a runner label
      # ever moves -- GitHub retires an image, someone repoints a PERF_RUNNER_*
      # variable at the wrong metal -- the run would otherwise publish timings
      # from one architecture under another one's name, which is worse than no
      # measurement at all.
      - name: Assert the host is the architecture it claims to be
        env:
          EXPECTED: ${{ matrix.target }}
        run: |
          set -Eeuo pipefail
          host="$(rustc -vV | sed -n 's/^host: //p')"
          echo "rustc host triple: ${host}"
          if [ "${host}" != "${EXPECTED}" ]; then
            echo "::error::runner is ${host}, but the matrix entry declares ${EXPECTED}."
            echo "::error::Benchmarks must run NATIVELY; a cross-compiled binary cannot execute here."
            exit 1
          fi

      # .cargo/config.toml pins `linker = "aarch64-linux-gnu-gcc"` for this
      # triple -- written for CROSS-compiling from x86_64, where that name is
      # the cross toolchain. On a native arm64 runner the same triple is the
      # host, and cargo still demands that exact binary name. Debian's multiarch
      # gcc usually provides it; when it does not, the native compiler IS the
      # right one and only the name is missing, so a symlink is the whole fix.
      - name: Provide the linker name .cargo/config.toml expects
        if: matrix.key == 'linux-aarch64'
        run: |
          set -Eeuo pipefail
          if command -v aarch64-linux-gnu-gcc >/dev/null 2>&1; then
            echo "aarch64-linux-gnu-gcc already on PATH: $(command -v aarch64-linux-gnu-gcc)"
            exit 0
          fi
          mkdir -p "$HOME/.local/bin"
          ln -sf "$(command -v gcc)" "$HOME/.local/bin/aarch64-linux-gnu-gcc"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          echo "linked aarch64-linux-gnu-gcc -> $(command -v gcc)"

      - name: Print runner profile
        run: |
          set -Eeuo pipefail
          echo "::group::Runner profile"
          uname -a
          rustc --version
          if [ "${RUNNER_OS}" = "Linux" ]; then
            echo "CPUs: $(nproc)"
            free -h | awk '/Mem:/ {print "Mem:  " $2}'
          else
            echo "CPUs: $(sysctl -n hw.ncpu)"
            echo "Mem:  $(( $(sysctl -n hw.memsize) / 1024 / 1024 / 1024 )) GiB"
            sw_vers
          fi
          echo "::endgroup::"

      # Linux-only and best-effort: macOS exposes no equivalent knob, and the
      # governor is not settable on a GitHub-hosted VM at all. It costs seconds
      # and matters on self-hosted metal.
      - name: Reduce CPU frequency variance
        if: runner.os == 'Linux'
        run: |
          sudo cpupower frequency-set --governor performance \
            || echo "cpupower unavailable; continuing with the default governor"

      # Records how busy the host was BEFORE the run, which is the only moment
      # the answer is meaningful. See the script's own header for 2026-08-01,
      # where 77 internally consistent measurements were 4.6x-20.9x wrong.
      #
      # `--report`, so a busy runner marks the data rather than failing the job:
      # on a shared runner the verdict is `degraded` often enough that a gate
      # would just be noise, and the marker is what the docs page keys its
      # warning banner off.
      - name: Host preflight
        id: preflight
        run: |
          set -Eeuo pipefail
          python3 scripts/bench-preflight.py --report --snapshot bench-host.json
          quality="$(python3 -c 'import json,pathlib;print(json.loads(pathlib.Path("bench-host.json").read_text())["quality"])')"

          # A GitHub-hosted runner is a shared VM whose wall-clock timings vary
          # by tens of percent between runs. A quiet load average says the
          # measurement was not contaminated by a NEIGHBOURING PROCESS; it says
          # nothing about the hypervisor. Only a host we control can claim `ok`.
          if [ "${RUNNER_ENVIRONMENT}" != "self-hosted" ]; then
            quality="degraded"
            echo "github-hosted runner: forcing quality=degraded"
          fi
          echo "quality=${quality}" >> "$GITHUB_OUTPUT"

      - name: Build benchmarks (warm cache, fail fast on compile errors)
        run: cargo bench --locked -p glaucus-bench --no-run

      - name: Run benchmarks
        env:
          BENCH: ${{ inputs.bench }}
          MODE: ${{ inputs.mode || 'full' }}
        run: |
          set -Eeuo pipefail
          cargo_args=(--locked -p glaucus-bench)
          if [ -n "${BENCH}" ]; then
            cargo_args+=(--bench "${BENCH}")
          fi
          # `--output-format bencher` is what bench-to-json.py and the
          # benchmark-action baseline both parse; `--quick` drops Criterion's
          # sample count for a fast smoke run and is NOT decision-grade.
          criterion_args=(--output-format bencher)
          if [ "${MODE}" = "quick" ]; then
            criterion_args+=(--quick)
          fi
          cargo bench "${cargo_args[@]}" -- "${criterion_args[@]}" | tee bench-output.txt

      - name: Sanity-check parseable output
        run: |
          set -Eeuo pipefail
          if ! grep -q "ns/iter" bench-output.txt; then
            echo "::error::No 'ns/iter' lines in bench-output.txt."
            echo "Criterion may have failed before producing measurements."
            tail -n 50 bench-output.txt
            exit 1
          fi
          echo "Parsed $(grep -c 'ns/iter' bench-output.txt) benchmark measurements."

      # One file per platform, named by the matrix key, so `perf-report` can
      # download every artifact into a single directory without collisions.
      - name: Render benchmark JSON
        env:
          KEY: ${{ matrix.key }}
          RUNNER_LABEL: ${{ matrix.label }}
          RUNNER_NAME: ${{ matrix.runner }}
          QUALITY: ${{ steps.preflight.outputs.quality }}
        run: |
          set -Eeuo pipefail
          python3 scripts/bench-to-json.py bench-output.txt \
            -o "perf-out/${KEY}.json" \
            --quality "${QUALITY}" \
            --runner "${RUNNER_LABEL}" \
            --note "Measured on ${RUNNER_NAME}. Ratios between libraries are the figures worth reading; absolute times are comparable only within this one platform's column."

      - name: Upload benchmark JSON
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: perf-json-${{ matrix.key }}
          path: perf-out/
          retention-days: 90
          if-no-files-found: warn

      - name: Upload bencher-format measurements
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: bench-output-${{ matrix.key }}-${{ github.run_id }}
          path: |
            bench-output.txt
            bench-host.json
          retention-days: 90
          if-no-files-found: warn

      - name: Upload Criterion HTML reports
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: criterion-reports-${{ matrix.key }}-${{ github.run_id }}
          path: target/criterion/
          retention-days: 30
          if-no-files-found: warn

  # ──────────────────────────────────────────────
  #  7.  Perf report — the cross-platform pivot
  # ──────────────────────────────────────────────
  # Without this the matrix leaves four artifacts and no comparison, which is
  # the one thing a multi-arch run exists to produce.
  #
  # Read the table DOWN a column, not across a row: absolute times from four
  # different machines (and two of them shared VMs) are not comparable with each
  # other. What a row does show is a benchmark that behaves structurally
  # differently on one architecture -- an aarch64 column that is 5x its
  # neighbours where every other row is 1.2x is a real signal.
  perf-report:
    name: Perf Report
    needs: [perf-matrix, benchmark]
    # `always()`, so three good platforms still report when a fourth dies; the
    # failed `benchmark` job keeps the run red on its own.
    if: always() && needs.benchmark.result != 'skipped'
    runs-on: ${{ vars.RUNS_ON_ARM64 || vars.RUNS_ON || 'ubuntu-latest' }}
    timeout-minutes: 10
    steps:
      - name: Download every platform's JSON
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: perf-json-*
          merge-multiple: true
          path: perf-out

      - name: Build the cross-platform summary
        run: |
          set -Eeuo pipefail
          shopt -s nullglob
          files=(perf-out/*.json)
          if [ "${#files[@]}" -eq 0 ]; then
            echo "::warning::no benchmark JSON was produced; nothing to report"
            exit 0
          fi
          echo "Merging ${#files[@]} platform result files."

          cat > pivot.jq <<'JQ'
          def us: (. / 1000 * 100 | round / 100 | tostring) + " µs";

          (map(.runner) | unique) as $cols
          | ([ .[] | .runner as $r | .measurements[] | {id, group, r: $r, ns: .ns_per_iter} ]
             | group_by(.id) | sort_by(.[0].group, .[0].id)) as $rows
          | [ "| Benchmark | " + ($cols | join(" | ")) + " |",
              "|:--|" + ($cols | map("--:") | join("|")) + "|" ]
            + [ $rows[]
                | . as $g
                | "| `" + $g[0].id + "` | "
                  + ( $cols
                      | map( . as $c | (($g[] | select(.r == $c) | .ns) // null)
                             | if . == null then "—" else us end )
                      | join(" | ") )
                  + " |" ]
          | .[]
          JQ

          {
            echo "### Cross-platform benchmark results"
            echo
            echo "| Platform | Toolchain | Quality | Measurements |"
            echo "|:--|:--|:--|--:|"
            jq -sr '.[] | "| \(.runner) | \(.toolchain) | \(.quality) | \(.measurements | length) |"' "${files[@]}"
            echo
            jq -sr -f pivot.jq "${files[@]}"
            echo
            echo "> Compare DOWN a column. Absolute times from different hosts are not"
            echo "> comparable, and every \`degraded\` row was measured on a shared VM."
          } >> "$GITHUB_STEP_SUMMARY"

          # One artifact carrying every platform, for anyone who wants to diff
          # two runs without downloading four files.
          jq -s --arg run "${GITHUB_RUN_ID}" --arg sha "${GITHUB_SHA}" \
            '{run_id: $run, commit: $sha, platforms: .}' \
            "${files[@]}" > perf-combined.json

      - name: Upload the combined result
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: perf-combined-${{ github.run_id }}
          path: perf-combined.json
          retention-days: 90
          if-no-files-found: warn

  # ──────────────────────────────────────────────
  #  The gh-pages baseline gate — deliberately NOT wired up
  # ──────────────────────────────────────────────
  # benchmark-action/github-action-benchmark used to run here as a single-host
  # regression gate (`alert-threshold: 150%`, `fail-threshold: 200%`), pushing
  # its history to `bench-data/` on gh-pages. Restoring it under a MATRIX needs
  # three changes, none of them cosmetic:
  #
  #   1. Per-platform storage. One `benchmark-data-dir-path` shared by four
  #      hosts interleaves incomparable series into one chart and alerts on the
  #      difference between machines. It has to become `bench-data/<matrix.key>`,
  #      with `name` carrying the platform too.
  #   2. Serialised pushes. Four jobs committing to gh-pages concurrently race;
  #      the loser's history is lost or the push fails outright. Either set
  #      `max-parallel: 1` on the matrix, or move the compare into `perf-report`
  #      and loop over the downloaded artifacts.
  #   3. `permissions: contents: write` on whichever job pushes -- which is why
  #      it is absent today. Every job in this file is read-only, and a write
  #      grant that exists for a disabled step is exactly the silent privilege
  #      this file's header note is about.
  #
  # Also unresolved: docs/benchmarks/results.data.ts reads ONE
  # docs/.data/benchmarks.json, so publishing four platforms means teaching the
  # loader about the platform dimension first. Until then `perf-combined.json`
  # from `perf-report` is the artifact to read.
